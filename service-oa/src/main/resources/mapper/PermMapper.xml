<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hualiang.core.mapper.PermMapper">

    <!-- getPermsByUserId --> 
    <select id="getPermsByUserId" resultType="Perm">
        select p.* from perm p
        inner join role_perm rp on p.pid = rp.perm_id
        inner join user u on rp.role_id = u.rid
        where u.uid = #{userId}
    </select>
</mapper>
